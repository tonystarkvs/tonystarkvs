=== AGENT COMMAND EXECUTION RULES & CONSTRAINTS ===

1. DO NOT RUN LOCAL SERVERS / DEPLOY SERVERS
   - Do not execute commands like `npm run dev`, `npm start`, python HTTP servers, or any command that attempts to start a local listener on localhost.
   - The user tests all changes locally and on staging. Agents must modify the workspace files statically and report completion in one go.

2. ONLY FIX RELEVANT ISSUE FILES
   - Focus editing strictly on the files directly causing or related to the reported issue.
   - Do not make broad edits or change regression test pages.

3. PRESERVE THE LAYOUT AND STYLING SCHEMES
   - Always take reference from the template `src/sdet-topics/selenium-java/selenium_s17_s19_cookies_broken_links.html`.
   - Use the designated container classes (`qi`, `qq`, `qa`, etc.) rather than introducing flat, ad-hoc classes.
